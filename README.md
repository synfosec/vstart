# vstart

This is a shell script I use to automate the process of configuring my VPS on spin-up

### Usage

```sh
sh -c 'wget -q "https://raw.githubusercontent.com/synfosec/vstart/main/scripts/vstart"; chmod +x ./vstart; ./vstart; rm ./vstart'
```
